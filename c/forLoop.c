#include <stdio.h>

int main()
{
    // init
    // comparison
    // update

    int max;
    printf("Insert an integer: ");
    scanf("%d", &max);

    for (int i = 1; i <= max; i++)
    {
        printf("%d\n", i);
    }

    return 0;
}
